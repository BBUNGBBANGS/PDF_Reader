#ifndef _LIBRARY
#define _LIBRARY

#include <stdint.h>

extern void fill(int length, int* buffer,int val);
extern void reverse(int length,int * buffer);
extern int Reduce(int length,int * buffer);
#endif